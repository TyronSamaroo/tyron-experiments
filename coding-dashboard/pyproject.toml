[project]
name = "coding-dashboard"
version = "0.1.0"
description = "A fun, lightweight CLI tool to track your coding progress and get motivated!"
requires-python = ">=3.9"
dependencies = [
    "pyfiglet>=1.0.2",
    "colorama>=0.4.6",
]
