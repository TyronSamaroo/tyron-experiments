[project]
name = "macro-tracker"
version = "0.1.0"
description = "A simple CLI tool to track weight, calories, and macros"
requires-python = ">=3.9"
dependencies = [
    "colorama>=0.4.6",
    "tabulate>=0.9.0",
]
