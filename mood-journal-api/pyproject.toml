[project]
name = "mood-journal-api"
version = "0.1.0"
description = "A small FastAPI service for logging daily mood entries."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.121.1",
    "sqlmodel>=0.0.27",
    "uvicorn>=0.38.0",
]
